/// vim:ft=jsonc

/// The position of this file:
/// - `~/.vim/`           (Vim    on Unix)
/// - `~/.config/nvim/`   (NeoVim on Unix)
/// - `~/vimfiles/`       (Vim    on Windows)

/// You can also call `:CocConfig` in Vim to open the configuration file.

{
  "inlayHint.enable": false,
  "semanticTokens.enable": true,
  "languageserver": {
    "erlang": {
      "command": "elp",
      "args": ["server"],
      "filetypes": ["erlang"],
      "rootpatterns": ["rebar.config", "rebar.lock", "Makefile", ".git"]
    }
  }
}
